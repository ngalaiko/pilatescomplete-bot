* validate cookie with api
* set secure cookie when https is terminated on the proxy (fly.io setup)
* log to file
* display errors in template
* htmlx
* caldev integration
* csrf
* drop curl dependency
